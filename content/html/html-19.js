/* ==================================================================
   Шаг html-19 · Итоговая работа модуля HTML
   Тип шага: assignment. Редактора нет: студент читает задание,
   запускает автопроверку опубликованного сайта, отмечает пункты
   самопроверки и сохраняет ссылки. Оценка ставится в Moodle.
   ================================================================== */

export default {
  title: 'Свой сайт на чистом HTML',
  type: 'assignment',
  points: 5,

  theory: `
<h2>Итоговая работа: сайт на чистом HTML</h2>
<p class="lead">Первая оцениваемая работа курса – 5 баллов. Вы создаёте небольшой сайт о своей предметной области и применяете всё, что изучили в модуле HTML. Работа сдаётся в Moodle двумя ссылками: на опубликованный сайт и на репозиторий. Срок сдачи указан в элементе «Задание» в Moodle.</p>

<h3>Задача</h3>
<p>Сайт из двух-трёх страниц: главная и одна-две внутренние, например «О проекте», «Материалы», «Запись». Страницы связаны общей навигацией. Главную страницу можно начать с кода, собранного в песочнице: в редакторе любого шага выделите код (Cmd+A или Ctrl+A) и скопируйте его – копирование из редактора разрешено. Замените учебные тексты своими и дополните страницу.</p>
<p>Пути к файлам курса вида <code>assets/img/globe.svg</code> в вашем репозитории работать не будут: этих файлов там нет. Положите свои изображения и медиафайлы в папку репозитория и укажите пути к ним или используйте полный адрес файла курса, например <code>https://oletol.github.io/HTML_Course/assets/media/lingua.mp4</code>. Встроенную страницу курса можно подключить так же, по полному адресу. Автопроверка покажет ссылки, которые не открываются.</p>

<h3>Требования</h3>
<p><strong>Структура.</strong> Каждая страница – полноценный документ: объявление типа, <code>lang</code>, <code>charset</code>, <code>viewport</code>, собственные <code>title</code> и <code>description</code>, значок. Страница разделена на <code>header</code> с навигацией, <code>main</code> и <code>footer</code>; навигация на каждой странице ведёт на все страницы сайта. Заголовки образуют иерархию без пропуска уровней.</p>
<p><strong>Содержание.</strong> Тексты о вашей предметной области, а не образцы из песочницы. На сайте есть списки, таблица с подписью, цитата, изображения с альтернативным текстом и размерами, хотя бы один медиафрагмент или встроенная страница.</p>
<p><strong>Взаимодействие без скриптов.</strong> Раскрывающиеся блоки <code>details</code> и форма с подписями полей и встроенной проверкой.</p>
<p><strong>Доступность и локализация.</strong> Ссылка «Перейти к основному содержанию», <code>lang</code> у иноязычных фрагментов, <code>abbr</code> или <code>time</code>, осмысленный текст ссылок, контраст текста и фона не ниже 4,5:1.</p>
<p><strong>Только HTML.</strong> Без файлов CSS, элемента <code>style</code> и скриптов. Атрибут <code>style</code> допускается только для <code>color</code> и <code>background-color</code>, как в шаге «Цвет».</p>
<p><strong>Публикация.</strong> Сайт опубликован на GitHub Pages. В репозитории не меньше пяти коммитов с понятными сообщениями. Ссылки между страницами относительные, имена файлов – латиницей, строчными буквами, без пробелов. Изображения лежат в папке репозитория, например <code>img/</code>.</p>

<h3>Порядок работы</h3>
<ol>
  <li>Создайте на GitHub новый репозиторий для работы, например <code>html-site</code>, и склонируйте его в VS Code.</li>
  <li>Создайте файлы <code>index.html</code> (главная) и остальные страницы, папку <code>img</code> для изображений.</li>
  <li>Работайте частями и фиксируйте каждую часть коммитом: скелет, навигация, содержание, форма, доработки.</li>
  <li>Включите публикацию: в репозитории Settings, раздел Pages, Source – Deploy from a branch, ветка <code>main</code>, папка <code>/ (root)</code>, кнопка Save. Через минуту-две сайт будет доступен по адресу <code>https://имя.github.io/репозиторий/</code>.</li>
  <li>Проверьте каждую страницу валидатором <code>validator.w3.org</code>: вкладка Validate by URI, адрес страницы. Исправьте все ошибки.</li>
  <li>Пройдите страницы клавишей Tab.</li>
  <li>Запустите автопроверку справа, исправьте отмеченное, отметьте пункты самопроверки и сохраните ссылки.</li>
  <li>Отправьте обе ссылки в задание в Moodle.</li>
</ol>

<h3>Критерии оценивания</h3>
<table class="rubric">
  <caption>5 баллов, по каждому критерию 0, 0,5 или 1 балл</caption>
  <thead>
    <tr><th scope="col">Критерий</th><th scope="col">1 балл</th><th scope="col">0,5 балла</th></tr>
  </thead>
  <tbody>
    <tr><th scope="row">Структура и семантика</th><td>Области, навигация, иерархия заголовков, списки и таблица размечены верно на всех страницах</td><td>Одна-две ошибки в структуре или семантике</td></tr>
    <tr><th scope="row">Содержание и связи</th><td>Собственные тексты по теме, работающие ссылки между страницами и на внешние источники, изображения с содержательным <code>alt</code></td><td>Часть текстов учебные или часть ссылок не работает</td></tr>
    <tr><th scope="row">Медиа и взаимодействие</th><td>Медиафрагмент или встраивание, <code>details</code>, форма с подписями и проверкой</td><td>Один из элементов отсутствует или размечен с ошибками</td></tr>
    <tr><th scope="row">Доступность и локализация</th><td>Ссылка на основное содержание, язык фрагментов, подписи полей, контраст, работа с клавиатуры</td><td>Одно-два нарушения доступности</td></tr>
    <tr><th scope="row">Код и публикация</th><td>Валидатор без ошибок, только HTML, сайт опубликован, не меньше пяти коммитов</td><td>Отдельные ошибки валидатора или меньше пяти коммитов</td></tr>
  </tbody>
</table>
<p>0 баллов по критерию – требования не выполнены. Если сайт не открывается по ссылке, работа не проверяется до исправления.</p>

<h3>Автопроверка</h3>
<p>Автопроверка справа загружает опубликованный сайт и проверяет формальные требования: наличие элементов, структуру, ограничения «только HTML». Она помогает найти пропуски до сдачи, но не выставляет оценку: качество текстов, осмысленность альтернативного текста и навигации оценивает преподаватель.</p>
`,

  checklist: [
    'Сайт состоит минимум из двух страниц, навигация на каждой ведёт на все страницы',
    'У каждой страницы свои `title` и `description`',
    'Использованы только средства HTML: нет CSS-файлов, элемента `style` и скриптов',
    'Все страницы проверены валидатором validator.w3.org, ошибок нет',
    'Страницы проходятся клавишей Tab в логическом порядке',
    'Изображения лежат в репозитории, открываются на опубликованном сайте, у файлов латинские имена без пробелов',
    'В репозитории не меньше пяти коммитов с понятными сообщениями',
    'Ссылки на сайт и репозиторий отправлены в задание в Moodle'
  ],

  checks: [],
  quiz: []
};
