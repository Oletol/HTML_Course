/* ==================================================================
   Шаг html-03 · Смысловое выделение
   Продолжает страницу студента из шага 2 (starter: 'previous').
   ================================================================== */

import prevStep from './html-02.js';
import { makeContext } from '../../js/validator.js';
import { baseFor, skeleton, texts, appendToBody } from './shared.js';

const inParagraph = (c, tag) => c.qa(`body p ${tag}`).filter(el => el.textContent.trim().length >= 2);

export default {
  title: 'Смысловое выделение',

  theory: `
<h2>Смысловое выделение</h2>
<p class="lead">Выделение в тексте бывает смысловым и визуальным. HTML описывает смысл: почему фрагмент выделен. Как он будет выглядеть – полужирным, курсивом или цветом, – решает CSS.</p>

<h3>Важность и логическое ударение</h3>
<p><code>strong</code> обозначает важность, серьёзность или срочность фрагмента: условие, предупреждение, ключевое ограничение. <code>em</code> обозначает логическое ударение, которое меняет смысл предложения.</p>
<pre><code>&lt;p&gt;&lt;strong&gt;Запись закрывается за сутки до занятия.&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Опыт работы с глиной &lt;em&gt;не&lt;/em&gt; требуется.&lt;/p&gt;</code></pre>
<p>Перенос ударения меняет высказывание: <code>&lt;em&gt;Мастер&lt;/em&gt; проведёт занятие</code> и <code>Мастер проведёт &lt;em&gt;занятие&lt;/em&gt;</code> отвечают на разные вопросы. Браузер по умолчанию показывает <code>strong</code> полужирным, а <code>em</code> курсивом, но эти начертания можно изменить в CSS, смысл при этом сохранится.</p>

<h3>Элементы b и i</h3>
<p><code>b</code> и <code>i</code> не устарели, но смысловой нагрузки важности не несут. <code>b</code> отмечает фрагмент, который принято выделять без дополнительной важности: ключевое слово в аннотации, название товара в обзоре. <code>i</code> отмечает текст иного характера: термин, иноязычное слово, мысль персонажа. Иноязычное слово размечается вместе с языком: <code>&lt;i lang="en"&gt;layout&lt;/i&gt;</code>. Выбор между <code>strong</code> и <code>b</code> определяется смыслом, а не внешним видом.</p>

<h3>Цитаты</h3>
<p><code>blockquote</code> – развёрнутая цитата из другого источника, отдельный блок. Внутри неё текст размечается абзацами. Необязательный атрибут <code>cite</code> содержит адрес источника, на странице он не отображается, поэтому автор указывается видимым текстом после цитаты.</p>
<pre><code>&lt;blockquote cite="https://example.org/interview"&gt;
  &lt;p&gt;Глина запоминает каждое движение рук.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Анна Петрова, руководитель мастерской&lt;/p&gt;</code></pre>
<p><code>q</code> – короткая цитата внутри предложения. Кавычки браузер добавляет сам по правилам языка страницы: для <code>lang="ru"</code> это «ёлочки», для <code>lang="en"</code> – “лапки”. Поэтому внутри <code>q</code> кавычки вручную не ставятся.</p>

<h3>Фрагменты кода</h3>
<p><code>code</code> обозначает фрагмент компьютерного кода в тексте: имя тега, название файла, команду. Многострочный код размещается в <code>pre</code>, который сохраняет пробелы и переводы строк: <code>&lt;pre&gt;&lt;code&gt;…&lt;/code&gt;&lt;/pre&gt;</code>.</p>

<div class="callout">
  <strong class="callout__title">Типичные ошибки</strong>
  <ul>
    <li><code>strong</code> охватывает весь абзац – выделение теряет смысл;</li>
    <li><code>blockquote</code> используется для отступа текста, а не для цитаты;</li>
    <li>кавычки поставлены вручную внутри <code>q</code> и на странице удваиваются;</li>
    <li>выбор между <code>strong</code> и <code>b</code> сделан по начертанию.</li>
  </ul>
</div>
`,

  task: [
    'Продолжите свою страницу. В абзацах своих разделов:',
    'выделите элементом `strong` одно важное условие или предупреждение (часть абзаца, а не весь абзац); выделите элементом `em` слово с логическим ударением; оформите элементом `q` короткую цитату внутри предложения, без кавычек вручную.',
    'Добавьте развёрнутую цитату `blockquote` с абзацем внутри – отзыв, высказывание специалиста или строку из источника по теме проекта – и сразу после неё абзац с указанием автора.'
  ],

  starter: 'previous',

  checks: [
    skeleton,
    {
      id: 'previous',
      label: 'Сохранена структура из шага 2: один `h1`, два `h2`, абзацы',
      test: c => c.qa('body h1').length === 1 && texts(c, 'body h2').length >= 2 && texts(c, 'body p', 10).length >= 2,
      where: () => null,
      hints: [
        'Задание шага 2 должно остаться выполненным: заголовки и абзацы не удаляются.',
        'Проверьте, что на странице по-прежнему один `h1`, не меньше двух `h2` и не меньше двух абзацев с текстом.',
        'Если структура нарушена, нажмите «Начать заново»: редактор откроется с кодом, сданным в шаге 2.'
      ]
    },
    {
      id: 'strong',
      label: 'В абзаце есть `strong`, который выделяет часть абзаца, а не весь',
      test: c => inParagraph(c, 'strong').some(s => {
        const p = s.closest('p');
        return s.textContent.trim().length < p.textContent.trim().length * 0.8;
      }),
      where: c => c.lineOf(/<strong\b/i),
      hints: [
        '`strong` должен находиться внутри абзаца `p` и охватывать только важную часть текста.',
        'Выберите в одном из абзацев условие или предупреждение и заключите только его в `<strong>` и `</strong>`.',
        'Образец: `<p>Занятие длится два часа. <strong>Опоздание больше 15 минут – перенос.</strong></p>`'
      ]
    },
    {
      id: 'em',
      label: 'В абзаце есть слово с логическим ударением в `em`',
      test: c => inParagraph(c, 'em').length > 0,
      where: c => c.lineOf(/<em\b/i),
      hints: [
        '`em` ставится внутри абзаца `p` вокруг слова, на которое падает логическое ударение.',
        'Выберите слово, ударение на котором меняет смысл предложения, и заключите его в `<em>` и `</em>`.',
        'Образец: `<p>Опыт работы с глиной <em>не</em> требуется.</p>`'
      ]
    },
    {
      id: 'q',
      label: 'В абзаце есть короткая цитата `q` без кавычек внутри',
      test: c => inParagraph(c, 'q').some(q => !/^[«"„“'‘]|[»"“”'’]$/.test(q.textContent.trim())),
      where: c => c.lineOf(/<q\b/i),
      hints: [
        'Короткая цитата размечается элементом `q` внутри абзаца. Кавычки браузер поставит сам.',
        'Проверьте, что `q` находится внутри `p` и что внутри `q` нет знаков « » или " ".',
        'Образец: `<p>Мастер повторяет: <q>глина прощает ошибки</q>.</p>`'
      ]
    },
    {
      id: 'blockquote',
      label: 'Есть цитата `blockquote` с абзацем `p` внутри',
      test: c => c.qa('body blockquote').some(b => [...b.querySelectorAll('p')].some(p => p.textContent.trim().length >= 10)),
      where: c => c.lineOf(/<blockquote\b/i),
      hints: [
        'Развёрнутая цитата – элемент `blockquote`. Текст внутри него размечается абзацами.',
        'Структура: открывающий `<blockquote>`, внутри абзац `<p>` с текстом цитаты, затем `</blockquote>`.',
        'Образец: `<blockquote>`, на следующей строке `<p>Глина запоминает каждое движение рук.</p>`, затем `</blockquote>`.'
      ]
    },
    {
      id: 'attribution',
      label: 'Сразу после `blockquote` идёт абзац с автором цитаты',
      test: c => c.qa('body blockquote').some(b => {
        const n = b.nextElementSibling;
        return n && n.tagName === 'P' && n.textContent.trim().length >= 3;
      }),
      where: c => c.lineOf(/<\/blockquote\s*>/i),
      hints: [
        'Атрибут `cite` не виден на странице, поэтому автор указывается обычным абзацем сразу после цитаты.',
        'Следующим элементом после `</blockquote>` должен быть `p` с именем автора или названием источника.',
        'Образец: после `</blockquote>` строка `<p>Анна Петрова, руководитель мастерской</p>`.'
      ]
    }
  ],

  solution: c0 => appendToBody(baseFor(c0, prevStep, makeContext), [
    '',
    '<p>Занятие длится два часа. <strong>Запись закрывается за сутки до начала.</strong></p>',
    '<p>Предварительная подготовка <em>не</em> требуется.</p>',
    '<p>Руководитель проекта повторяет: <q>лучше один раз попробовать самому</q>.</p>',
    '<blockquote>',
    '  <p>Текст развёрнутой цитаты по теме проекта.</p>',
    '</blockquote>',
    '<p>Автор цитаты, должность или источник</p>'
  ]),

  quiz: [
    {
      q: 'Чем `strong` отличается от `b`?',
      options: [
        '`strong` означает важность, `b` – выделение без важности',
        '`strong` даёт жирное начертание, `b` – полужирное и светлее',
        '`strong` устарел, и вместо него сейчас рекомендуется `b`',
        '`strong` работает только в абзаце, а `b` – в любом элементе'
      ],
      correct: 0,
      explain: 'Оба по умолчанию выглядят одинаково. Разница в смысле: `strong` сообщает о важности, `b` выделяет фрагмент по традиции оформления.'
    },
    {
      q: 'В предложении «Я просил купить молоко, а не кефир» ударение падает на слово «молоко». Какой элемент подходит для этого слова?',
      options: ['`em`', '`strong`', '`i`', '`b`'],
      correct: 0,
      explain: 'Логическое ударение, меняющее смысл предложения, размечается `em`. `strong` обозначал бы важность, `i` – иной характер текста.'
    },
    {
      q: 'Где указывают автора цитаты, размеченной `blockquote`?',
      options: [
        'Видимым текстом после цитаты',
        'В атрибуте `cite`, он виден под цитатой',
        'В атрибуте `title` элемента `blockquote`',
        'Автора в HTML указывать не принято'
      ],
      correct: 0,
      explain: 'Атрибут `cite` хранит адрес источника, но не отображается. `title` виден только при наведении мыши и недоступен на сенсорных экранах.'
    },
    {
      q: 'Почему внутри `q` не ставят кавычки вручную?',
      options: [
        'Браузер добавит кавычки сам по языку страницы',
        'Валидатор HTML запрещает кавычки внутри `q`',
        'Элемент `q` выводит цитату курсивом без кавычек',
        'Кавычки внутри `q` нарушают кодировку страницы'
      ],
      correct: 0,
      explain: 'Кавычки для `q` задаются браузером по атрибуту `lang`: для русского текста «ёлочки». Ручные кавычки удвоятся.'
    },
    {
      q: 'Какой элемент подходит для имени тега в тексте, например «элемент p»?',
      options: ['`code`', '`em`', '`q`', '`strong`'],
      correct: 0,
      explain: '`code` обозначает фрагмент кода в тексте: имя тега, файла или команды.'
    }
  ],

  passScore: 0.8
};
