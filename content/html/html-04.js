/* ==================================================================
   Шаг html-04 · Списки
   Продолжает страницу студента из шага 3 (starter: 'previous').
   ================================================================== */

import prevStep from './html-03.js';
import { makeContext } from '../../js/validator.js';
import { baseFor, skeleton, texts, appendToBody } from './shared.js';

const itemsOf = list => [...list.children].filter(el => el.tagName === 'LI' && el.textContent.trim());
const LIST_IN_P = /<p\b[^>]*>(?:(?!<\/p\s*>|<p\b)[\s\S])*?<(ul|ol)\b/i;
const FAKE_ITEM = /^\s*([-–—•*·]|\d+[.)])\s+/;

export default {
  title: 'Списки',

  theory: `
<h2>Списки</h2>
<p class="lead">Список объединяет однородные пункты и сообщает о них больше, чем набор строк: браузер нумерует пункты, а программа экранного доступа объявляет «список, 4 пункта» и позволяет переходить от пункта к пункту.</p>

<h3>Маркированный и нумерованный списки</h3>
<p><code>ul</code> (unordered list) – маркированный список: порядок пунктов не имеет значения. <code>ol</code> (ordered list) – нумерованный: порядок значим, например шаги инструкции или места в рейтинге. Каждый пункт – элемент <code>li</code> (list item).</p>
<pre><code>&lt;ul&gt;
  &lt;li&gt;глина и инструменты&lt;/li&gt;
  &lt;li&gt;фартук&lt;/li&gt;
  &lt;li&gt;обжиг одного изделия&lt;/li&gt;
&lt;/ul&gt;

&lt;ol&gt;
  &lt;li&gt;Выберите дату занятия.&lt;/li&gt;
  &lt;li&gt;Заполните форму записи.&lt;/li&gt;
  &lt;li&gt;Дождитесь подтверждения.&lt;/li&gt;
&lt;/ol&gt;</code></pre>
<p>Прямыми потомками <code>ul</code> и <code>ol</code> могут быть только элементы <code>li</code>. Текст, заголовки и абзацы размещаются внутри пунктов.</p>

<h3>Вложенные списки</h3>
<p>Вложенный список находится внутри элемента <code>li</code> родительского списка – после текста пункта и до его закрывающего тега.</p>
<pre><code>&lt;ul&gt;
  &lt;li&gt;Материалы
    &lt;ul&gt;
      &lt;li&gt;белая глина&lt;/li&gt;
      &lt;li&gt;шамотная глина&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;Инструменты&lt;/li&gt;
&lt;/ul&gt;</code></pre>

<h3>Атрибуты нумерованного списка</h3>
<ul>
  <li><code>start="5"</code> – нумерация начинается с пяти;</li>
  <li><code>reversed</code> – обратный порядок нумерации;</li>
  <li><code>type="a"</code>, <code>"A"</code>, <code>"i"</code>, <code>"I"</code> – буквы или римские цифры вместо арабских, когда тип нумерации значим для содержания, например в тексте договора.</li>
</ul>

<h3>Список описаний</h3>
<p>Для пар «термин – определение» служит список описаний <code>dl</code>: термин размечается <code>dt</code>, описание – <code>dd</code>. Он уместен в глоссарии, в характеристиках товара, в перечне вопросов и ответов.</p>

<h3>Списки и абзацы</h3>
<p>Список не может находиться внутри абзаца: встретив <code>ul</code> или <code>ol</code>, браузер закрывает открытый <code>p</code>. Поясняющий текст к списку оформляется отдельным абзацем перед ним.</p>

<div class="callout">
  <strong class="callout__title">Типичные ошибки</strong>
  <ul>
    <li>перечень из абзацев, начинающихся с дефиса или номера, вместо списка;</li>
    <li>вложенный список помещён прямо в <code>ul</code>, а не в <code>li</code>;</li>
    <li>текст или абзац стоит между пунктами непосредственно в <code>ul</code>;</li>
    <li>нумерованный список выбран ради цифр, хотя порядок пунктов не важен.</li>
  </ul>
</div>
`,

  task: [
    'Продолжите свою страницу.',
    'Добавьте в подходящий раздел маркированный список `ul` не меньше чем из трёх пунктов: например, что входит в услугу или какие темы охватывает проект. Добавьте нумерованный список `ol` не меньше чем из трёх шагов: порядок действий для посетителя. В одном из пунктов сделайте вложенный список. Перед каждым списком поставьте поясняющий абзац.'
  ],

  starter: 'previous',

  checks: [
    skeleton,
    {
      id: 'previous',
      label: 'Сохранена структура предыдущих шагов: один `h1`, два `h2`, абзацы',
      test: c => c.qa('body h1').length === 1 && texts(c, 'body h2').length >= 2 && texts(c, 'body p', 10).length >= 2,
      where: () => null,
      hints: [
        'Заголовки и абзацы из прошлых шагов должны остаться на странице.',
        'Проверьте, что на странице один `h1`, не меньше двух `h2` и не меньше двух абзацев с текстом.',
        'Если структура нарушена, нажмите «Начать заново»: редактор откроется с кодом, сданным в шаге 3.'
      ]
    },
    {
      id: 'ul',
      label: 'Есть маркированный список `ul` не меньше чем из трёх пунктов',
      test: c => c.qa('body ul').some(l => itemsOf(l).length >= 3),
      where: c => c.lineOf(/<ul\b/i),
      hints: [
        'Нужен список, в котором порядок пунктов не важен, – элемент `ul`, и в нём не меньше трёх `li` с текстом.',
        'Каждый пункт – отдельный парный тег `li` непосредственно внутри `ul`.',
        'Образец: `<ul>`, затем три строки вида `<li>фартук</li>`, затем `</ul>`.'
      ]
    },
    {
      id: 'ol',
      label: 'Есть нумерованный список `ol` не меньше чем из трёх пунктов',
      test: c => c.qa('body ol').some(l => itemsOf(l).length >= 3),
      where: c => c.lineOf(/<ol\b/i),
      hints: [
        'Нужен список, в котором важен порядок, – элемент `ol`, и в нём не меньше трёх `li`.',
        'Цифры писать не нужно: браузер пронумерует пункты сам.',
        'Образец: `<ol>`, затем `<li>Выберите дату занятия.</li>` и ещё два шага, затем `</ol>`.'
      ]
    },
    {
      id: 'nested',
      label: 'В одном из пунктов есть вложенный список',
      test: c => c.qa('body li > ul, body li > ol').some(l => itemsOf(l).length >= 1),
      where: c => null,
      hints: [
        'Вложенный список должен находиться внутри элемента `li`, а не рядом с ним.',
        'Откройте пункт `<li>`, напишите его текст, затем целый список `<ul>…</ul>`, и только после этого закройте `</li>`.',
        'Образец: `<li>Материалы <ul><li>белая глина</li><li>шамотная глина</li></ul></li>` – можно на нескольких строках.'
      ]
    },
    {
      id: 'onlyLi',
      label: 'Внутри `ul` и `ol` непосредственно стоят только пункты `li`',
      test: c => c.qa('body ul, body ol').every(l => [...l.childNodes].every(n =>
        (n.nodeType === 1 && n.tagName === 'LI') || (n.nodeType === 3 && !n.textContent.trim()) || n.nodeType === 8)),
      where: c => {
        const bad = c.qa('body ul, body ol').find(l => [...l.children].some(ch => ch.tagName !== 'LI'));
        if (!bad) return null;
        const child = [...bad.children].find(ch => ch.tagName !== 'LI');
        return c.lineOf(new RegExp(`<${child.tagName.toLowerCase()}\\b`, 'i'));
      },
      hints: [
        'Между пунктами списка оказался текст или другой элемент. Непосредственно в `ul` и `ol` допустимы только `li`.',
        'Если это вложенный список, перенесите его внутрь предыдущего `li` – до тега `</li>`. Если это текст, заключите его в `li` или вынесите в абзац перед списком.',
        'Неверно: `<ul><li>Материалы</li><ul>…</ul></ul>`. Верно: `<ul><li>Материалы<ul>…</ul></li></ul>`.'
      ]
    },
    {
      id: 'notInP',
      label: 'Списки не находятся внутри абзацев',
      test: c => !LIST_IN_P.test(c.clean),
      where: c => c.lineOf(LIST_IN_P),
      hints: [
        'Список открыт внутри незакрытого абзаца `p`. Браузер в этом месте закрывает абзац сам, и структура нарушается.',
        'Закройте абзац с поясняющим текстом тегом `</p>` до начала списка.',
        'Образец: `<p>В стоимость входит:</p>`, затем на следующей строке `<ul>`.'
      ]
    },
    {
      id: 'noFakeLists',
      label: 'Нет абзацев, начинающихся с дефиса, маркера или номера',
      test: c => !c.qa('body p').some(p => FAKE_ITEM.test(p.textContent)),
      where: c => {
        const p = c.qa('body p').find(el => FAKE_ITEM.test(el.textContent));
        if (!p) return null;
        const i = c.clean.indexOf(p.textContent.trim().slice(0, 20));
        return i >= 0 ? c.clean.slice(0, i).split('\n').length : null;
      },
      hints: [
        'Абзац, начинающийся с дефиса или номера, выглядит как пункт списка, но списком не является.',
        'Соберите такие абзацы в `ul` или `ol`: каждый станет элементом `li`, а дефисы и номера нужно убрать.',
        'Было: `<p>– фартук</p>`. Стало: `<li>фартук</li>` внутри `<ul>`.'
      ]
    }
  ],

  solution: c0 => appendToBody(baseFor(c0, prevStep, makeContext), [
    '',
    '<p>В стоимость входит:</p>',
    '<ul>',
    '  <li>материалы',
    '    <ul>',
    '      <li>основной материал</li>',
    '      <li>дополнительные материалы</li>',
    '    </ul>',
    '  </li>',
    '  <li>инструменты</li>',
    '  <li>консультация специалиста</li>',
    '</ul>',
    '',
    '<p>Как принять участие:</p>',
    '<ol>',
    '  <li>Выберите удобную дату.</li>',
    '  <li>Заполните форму записи.</li>',
    '  <li>Дождитесь подтверждения.</li>',
    '</ol>'
  ]),

  quiz: [
    {
      q: 'Нужно перечислить шаги записи на занятие. Какой список выбрать?',
      options: [
        'Нумерованный `ol`',
        'Маркированный `ul`',
        'Список описаний `dl`',
        'Несколько абзацев `p`'
      ],
      correct: 0,
      explain: 'Порядок шагов значим, поэтому нужен `ol`. Нумерацию браузер проставит сам.'
    },
    {
      q: 'Где размещают вложенный список?',
      options: [
        'Внутри элемента `li` родительского списка',
        'Прямо внутри `ul`, между пунктами списка',
        'Сразу после закрывающего тега списка',
        'В отдельном абзаце `p` под пунктом списка'
      ],
      correct: 0,
      explain: 'Вложенный список уточняет конкретный пункт, поэтому он находится внутри этого `li`, до его закрывающего тега.'
    },
    {
      q: 'Что может быть прямым потомком `ul`?',
      options: [
        'Только элементы `li`',
        'Любые элементы и текст',
        'Элементы `li` и абзацы `p`',
        'Заголовки и элементы `li`'
      ],
      correct: 0,
      explain: 'Непосредственно в `ul` и `ol` допустимы только `li`. Абзацы, заголовки и вложенные списки размещаются внутри пунктов.'
    },
    {
      q: 'Почему перечень не оформляют абзацами с дефисами в начале?',
      options: [
        'Программа экранного доступа не распознает его как список',
        'Валидатор HTML считает дефис в начале абзаца ошибкой',
        'Браузер удаляет дефисы в начале абзацев при отображении',
        'Абзацы с дефисами нельзя будет оформить средствами CSS позже'
      ],
      correct: 0,
      explain: 'Внешне результат похож, но в структуре документа списка нет: не объявляется число пунктов и недоступна навигация по ним.'
    },
    {
      q: 'Какой атрибут начинает нумерацию `ol` с пяти?',
      options: ['`start="5"`', '`value="5"`', '`begin="5"`', '`from="5"`'],
      correct: 0,
      explain: '`start` задаёт начальный номер списка. Атрибут `value` существует, но ставится на отдельный `li` и меняет номер конкретного пункта.'
    }
  ],

  passScore: 0.8
};
