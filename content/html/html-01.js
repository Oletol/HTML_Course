/* ==================================================================
   Шаг html-01 · Скелет документа

   Структура файла шага (образец для всех следующих шагов):
     title      – название
     theory     – HTML левой панели (доверенный текст, пишем сами)
     task       – абзацы задания; `в обратных кавычках` – код
     starter    – код, с которым открывается редактор
     checks     – требования: id, label, test(ctx), where(ctx), hints[3]
                  where – строка для подсветки, если требование не выполнено
     solution   – правильный код; вставляется после 7-й неудачной проверки
     quiz       – вопросы: q, options, correct (индекс), explain
     passScore  – доля верных ответов для прохождения теста

   В текстах: без стрелок, вместо длинного тире – короткое (–).
   ================================================================== */

export default {
  title: 'Скелет документа',

  theory: `
<h2>Скелет документа</h2>
<p class="lead">Любая веб-страница – это текстовый файл. Браузер читает его и строит дерево элементов. Основа этого дерева одинакова для всех страниц: объявление типа документа, корневой элемент, служебный раздел <code>head</code> и видимый раздел <code>body</code>.</p>

<pre><code>&lt;!DOCTYPE html&gt;
&lt;html lang="ru"&gt;
  &lt;head&gt;
    &lt;meta charset="utf-8"&gt;
    &lt;meta name="viewport" content="width=device-width, initial-scale=1"&gt;
    &lt;title&gt;Пробное занятие – Мастерская керамики&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    Мастерская керамики на Васильевском
  &lt;/body&gt;
&lt;/html&gt;</code></pre>

<h3>Обязательные элементы документа</h3>
<ul class="parts">
  <li><code class="parts__tag">&lt;!DOCTYPE html&gt;</code>
    Объявление типа документа, а не тег. Сообщает браузеру, что документ написан на современном HTML и отображается по стандарту. Без него браузер включает режим совместимости со страницами 1990-х годов и иначе рассчитывает размеры блоков.</li>
  <li><code class="parts__tag">&lt;html lang="ru"&gt;</code>
    Корень документа, внутри него всё остальное. Атрибут <code>lang</code> называет язык текста. По нему скринридер выбирает произношение, браузер – правила переносов, а встроенный переводчик понимает, с какого языка переводить. Для русского текста – <code>ru</code>, для английского – <code>en</code>.</li>
  <li><code class="parts__tag">&lt;head&gt;</code>
    Служебная часть. Её содержимое не видно на странице: здесь сведения о документе для браузера, поисковых систем и соцсетей.</li>
  <li><code class="parts__tag">&lt;meta charset="utf-8"&gt;</code>
    Кодировка. Ставится первой строкой внутри <code>head</code>: браузер должен узнать её прежде, чем встретит первую кириллическую букву. Иначе текст может отобразиться нечитаемыми символами.</li>
  <li><code class="parts__tag">&lt;meta name="viewport" …&gt;</code>
    Настройка для телефонов. Без неё смартфон отрисовывает страницу шириной около 980 пикселей и уменьшает её целиком, текст становится мелким. Полная запись: <code>content="width=device-width, initial-scale=1"</code>. Это фундамент адаптивной вёрстки, к которой мы придём в модуле CSS.</li>
  <li><code class="parts__tag">&lt;title&gt;</code>
    Название документа. Его видно во вкладке браузера, в закладках и в результатах поиска. Сначала суть страницы, потом название сайта.</li>
  <li><code class="parts__tag">&lt;body&gt;</code>
    Тело документа: всё, что видит пользователь.</li>
</ul>

<h3>Вложенность и отступы</h3>
<p>Элементы вкладываются друг в друга: <code>head</code> и <code>body</code> лежат внутри <code>html</code>, а <code>meta</code> и <code>title</code> – внутри <code>head</code>. Закрываются они в обратном порядке: что открыли последним, закрываем первым.</p>
<p>Отступы предназначены для человека и не влияют на отображение. Вложенный элемент принято сдвигать на два пробела вправо, чтобы структура документа читалась без усилий. В редакторе клавиша Tab ставит два пробела, а Enter сохраняет отступ предыдущей строки.</p>
<p>У <code>meta</code> нет содержимого, поэтому у неё только открывающий тег. Такие элементы называют пустыми. Закрывающий <code>&lt;/meta&gt;</code> писать не нужно.</p>

<h3>Восстановление разметки браузером</h3>
<p>Если в коде отсутствуют <code>head</code> или <code>body</code>, браузер достраивает их самостоятельно, и страница в окне результата может выглядеть корректно. Проверка в песочнице анализирует исходный код, а не отображение, поэтому элементы, достроенные браузером, не засчитываются. Так же работает валидатор разметки.</p>

<div class="callout">
  <strong class="callout__title">Типичные ошибки</strong>
  <ul>
    <li><code>title</code> оказался внутри <code>body</code> – браузер покажет его текстом на странице, а вкладка останется без названия;</li>
    <li><code>meta charset</code> стоит после <code>title</code> – кириллица в названии может сломаться;</li>
    <li><code>lang="en"</code> у русского текста – скринридер прочитает русские слова с английским произношением;</li>
    <li>в <code>title</code> осталось слово Document – так его подставляют заготовки редакторов кода.</li>
  </ul>
</div>

<div class="callout callout--code">
  <strong class="callout__title">Порядок работы в песочнице</strong>
  <p>Код набирается только вручную: вставка и перетаскивание в редакторе отключены, поскольку набор закрепляет синтаксис. Окно результата обновляется автоматически, кнопка «Проверить» сверяет код с требованиями задания. После третьей и пятой неудачной проверки открываются подсказки, после седьмой в редактор вставляется правильный вариант с отмеченными отличиями.</p>
</div>
`,

  task: [
    'Наберите с нуля скелет главной страницы своего проекта – той предметной области, которую вы выбрали на первом занятии.',
    'В `body` пока достаточно одной строки текста, например названия проекта. Содержание появится в следующих шагах.'
  ],

  starter: '',

  /* Правильный вариант. Если студент уже написал своё название и текст,
     они сохраняются – тогда подсветка покажет только настоящие ошибки. */
  solution: c => {
    const title = (c.q('title')?.textContent || '').trim();
    const body = (c.clean.match(/<body[^>]*>([\s\S]*?)<\/body\s*>/i)?.[1] || '')
      .replace(/<[^>]*>/g, '').trim().split('\n')[0].trim();
    const safe = t => t.replace(/[<>]/g, '');
    return `<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>${title.length >= 3 && !/^document$/i.test(title) ? safe(title) : 'Главная – Мой проект'}</title>
  </head>
  <body>
    ${body ? safe(body) : 'Мой проект'}
  </body>
</html>`;
  },

  checks: [
    {
      id: 'doctype',
      label: 'Документ начинается с объявления `<!DOCTYPE html>`',
      test: c => /^\s*<!doctype\s+html\s*>/i.test(c.clean),
      where: c => c.lineOf(/<!doctype[^>]*>/i) ?? 1,
      hints: [
        'Первой строкой файла должно стоять объявление типа документа. Перед ним допустимы только пробелы и пустые строки.',
        'Объявление начинается с восклицательного знака и пишется в угловых скобках, как тег. Закрывающей пары у него нет.',
        'Образец первой строки: `<!DOCTYPE html>`'
      ]
    },
    {
      id: 'lang',
      label: 'У `<html>` указан язык: `lang="ru"`',
      test: c => /<html\b[^>]*\blang\s*=\s*["']?ru\b/i.test(c.clean) &&
        /^ru(-|$)/i.test(c.doc.documentElement.getAttribute('lang') || ''),
      where: c => c.lineOf(/<html\b/i),
      hints: [
        'Язык указывается атрибутом открывающего тега `<html>`, а не отдельным тегом.',
        'Атрибут пишется внутри угловых скобок после имени тега, через пробел: имя, знак равенства и значение в кавычках.',
        'Образец: `<html lang="ru">`. Закрывающий `</html>` стоит последней строкой файла.'
      ]
    },
    {
      id: 'structure',
      label: '`<head>` и `<body>` написаны явно, закрыты и идут в этом порядке внутри `<html>`',
      test: c => c.wrote('html') && c.closed('html') &&
        c.wrote('head') && c.closed('head') &&
        c.wrote('body') && c.closed('body') &&
        c.pos(/<\/head\s*>/i) < c.pos(/<body(?=[\s>])/i),
      hints: [
        'Проверьте, что написаны все шесть тегов: открывающие и закрывающие `html`, `head` и `body`.',
        'Порядок такой: сначала `<html>`, внутри него `<head>…</head>`, затем `<body>…</body>`, и в самом конце `</html>`. Голова закрывается до того, как открывается тело.',
        'Каркас без содержимого: `<html lang="ru">`, `<head>`, `</head>`, `<body>`, `</body>`, `</html>` – каждый на своей строке.'
      ]
    },
    {
      id: 'charset',
      label: 'Первой строкой внутри `<head>` стоит `<meta charset="utf-8">`',
      test: c => {
        const first = c.doc.head?.firstElementChild;
        return Boolean(first && first.matches('meta[charset]') &&
          /^utf-8$/i.test(first.getAttribute('charset').trim()));
      },
      where: c => c.lineOf(/<meta[^>]*charset/i) ?? c.lineOf(/<head\b/i),
      hints: [
        'Кодировку задаёт тег `meta` с атрибутом `charset`. Он должен быть самым первым элементом в `head`.',
        'Значение атрибута – название кодировки `utf-8`. У тега `meta` нет закрывающей пары.',
        'Образец: `<meta charset="utf-8">` сразу после строки `<head>`.'
      ]
    },
    {
      id: 'viewport',
      label: 'В `<head>` есть `<meta name="viewport">` со значением `width=device-width, initial-scale=1`',
      test: c => {
        const m = c.q('head > meta[name="viewport" i]');
        const content = (m?.getAttribute('content') || '').replace(/\s+/g, '');
        return /width=device-width/i.test(content) && /initial-scale=1(\.0)?(,|$)/i.test(content);
      },
      where: c => c.lineOf(/<meta[^>]*name\s*=\s*["']?view/i) ?? c.lineOf(/<\/head/i),
      hints: [
        'Нужен второй тег `meta` в `head`: у него два атрибута, `name` и `content`.',
        'Атрибут `name` называет настройку: `viewport`. В `content` через запятую две пары: ширина равна ширине устройства и начальный масштаб 1.',
        'Образец: `<meta name="viewport" content="width=device-width, initial-scale=1">`'
      ]
    },
    {
      id: 'title',
      label: 'В `<head>` есть `<title>` со своим названием, не Document',
      test: c => {
        const t = c.q('head > title');
        const text = (t?.textContent || '').trim();
        return text.length >= 3 && !/^document$/i.test(text);
      },
      where: c => c.lineOf(/<title\b/i),
      hints: [
        'Тег `title` должен стоять внутри `head`, а не внутри `body`, и содержать осмысленное название.',
        '`title` – парный тег: между открывающим и закрывающим тегом пишется название. Сначала суть страницы, потом имя проекта.',
        'Образец: `<title>Главная – Название вашего проекта</title>`'
      ]
    },
    {
      id: 'bodyText',
      label: 'В `<body>` есть хотя бы одна строка текста',
      test: c => {
        const b = c.clean.match(/<body[^>]*>([\s\S]*?)<\/body\s*>/i);
        return Boolean(b && b[1].replace(/<[^>]*>/g, '').trim().length > 0);
      },
      where: c => c.lineOf(/<body\b/i),
      hints: [
        'Всё, что видно на странице, пишется между `<body>` и `</body>`.',
        'Пока не нужны никакие теги – достаточно обычного текста, например названия вашего проекта.',
        'Образец: строка `Мастерская керамики на Васильевском` между `<body>` и `</body>`.'
      ]
    }
  ],

  quiz: [
    {
      q: 'Что произойдёт, если убрать `<!DOCTYPE html>`?',
      options: [
        'Браузер перейдёт в режим совместимости со старыми страницами',
        'Браузер откажется открывать страницу и покажет сообщение об ошибке',
        'Браузер удалит из документа всё содержимое элемента `body` целиком',
        'Ничего не изменится: объявление учитывает только валидатор разметки'
      ],
      correct: 0,
      explain: 'Без объявления браузер переходит в режим совместимости (quirks mode). Страница откроется, но часть правил вёрстки будет работать по устаревшим алгоритмам.'
    },
    {
      q: 'Где располагается `<meta charset="utf-8">`?',
      options: [
        'Первым элементом внутри `head`',
        'В любом месте внутри элемента `head`',
        'Первой строкой внутри элемента `body`',
        'Перед объявлением `<!DOCTYPE html>`'
      ],
      correct: 0,
      explain: 'Браузер должен узнать кодировку раньше, чем встретит первый символ кириллицы, в том числе в `title`. Поэтому `charset` идёт первым в `head`.'
    },
    {
      q: 'Для чего нужен атрибут `lang="ru"`?',
      options: [
        'Для выбора произношения, правил переносов и языка перевода',
        'Для отображения кириллицы: без него буквы искажаются',
        'Для поисковых систем: без него страница не индексируется',
        'Ни для чего: браузер определяет язык страницы самостоятельно'
      ],
      correct: 0,
      explain: 'По `lang` программа экранного доступа выбирает произношение, браузер – правила переносов и кавычек, переводчик – исходный язык. За отображение кириллицы отвечает кодировка, а не язык.'
    },
    {
      q: 'Где отображается содержимое `title`?',
      options: [
        'Во вкладке браузера, в закладках и в поисковой выдаче',
        'Крупным заголовком в самом верху видимой части страницы',
        'Только в исходном коде, пользователю оно не показывается',
        'В адресной строке браузера на месте адреса страницы'
      ],
      correct: 0,
      explain: 'Видимый заголовок страницы – другой элемент, `h1`; он рассматривается в следующем шаге. `title` описывает документ целиком и находится вне видимой части.'
    },
    {
      q: 'Что произойдёт на смартфоне, если не указать `meta viewport`?',
      options: [
        'Страница отрисуется как на широком экране и уменьшится',
        'Страница не откроется в мобильном браузере, только на компьютере',
        'Браузер сам перестроит страницу под ширину экрана телефона',
        'Со страницы пропадут все изображения и вложенные элементы'
      ],
      correct: 0,
      explain: 'Без `viewport` смартфон принимает ширину страницы около 980 пикселей и масштабирует её. С `width=device-width` ширина страницы равна ширине экрана, на этом строится адаптивная вёрстка.'
    }
  ],

  passScore: 0.8
};
